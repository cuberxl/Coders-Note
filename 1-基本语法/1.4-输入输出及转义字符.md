---


---

<h3 id="输入输出及转义字符"><span class="prefix"></span><span class="content">输入输出及转义字符</span><span class="suffix"></span></h3>
<hr>
<p>C++ 支持两种输入输出方式：流式输入输出（<code>cin</code>/<code>cout</code>）和 C 风格格式化输入输出（<code>scanf</code>/<code>printf</code>）。</p>
<h4 id="cin--cout（流式-io）"><span class="prefix"></span><span class="content">cin / cout（流式 I/O）</span><span class="suffix"></span></h4>
<p><code>cin</code> 是标准输入流对象，<code>cout</code> 是标准输出流对象，使用 <code>&gt;&gt;</code> 和 <code>&lt;&lt;</code> 运算符进行数据读写，自动根据变量类型处理格式。</p>
<pre><code class="prism language-cpp"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;iostream&gt;</span></span>
<span class="token keyword">using</span> <span class="token keyword">namespace</span> std<span class="token punctuation">;</span>

<span class="token keyword">int</span> a<span class="token punctuation">;</span>
<span class="token keyword">double</span> b<span class="token punctuation">;</span>
string s<span class="token punctuation">;</span>
cin <span class="token operator">&gt;&gt;</span> a <span class="token operator">&gt;&gt;</span> b <span class="token operator">&gt;&gt;</span> s<span class="token punctuation">;</span>     <span class="token comment">// 读取三个数据</span>
cout <span class="token operator">&lt;&lt;</span> <span class="token string">"a="</span> <span class="token operator">&lt;&lt;</span> a <span class="token operator">&lt;&lt;</span> <span class="token string">", b="</span> <span class="token operator">&lt;&lt;</span> b <span class="token operator">&lt;&lt;</span> <span class="token string">", s="</span> <span class="token operator">&lt;&lt;</span> s <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>
</code></pre>
<p><strong>注意</strong>：<code>cin</code> 默认以空白字符（空格、Tab、换行）作为分隔符，读取字符串时遇空白停止。</p>
<h4 id="cin--cout-常用成员函数"><span class="prefix"></span><span class="content">cin / cout 常用成员函数</span><span class="suffix"></span></h4>

<table>
<thead>
<tr>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>cin.get()</code></td>
<td>读取一个字符（包括空白符），返回 int</td>
</tr>
<tr>
<td><code>cin.getline(char*, size)</code></td>
<td>读取一行到 C 风格字符串，遇换行停止</td>
</tr>
<tr>
<td><code>cin.ignore(n, c)</code></td>
<td>忽略 n 个字符，或遇到字符 c 时停止</td>
</tr>
<tr>
<td><code>cin.peek()</code></td>
<td>查看下一个字符但不提取</td>
</tr>
<tr>
<td><code>cin.putback(ch)</code></td>
<td>将一个字符放回流中</td>
</tr>
<tr>
<td><code>cout.put(ch)</code></td>
<td>输出一个字符</td>
</tr>
<tr>
<td><code>cout.write(str, len)</code></td>
<td>输出 C 风格字符串的前 len 个字符</td>
</tr>
<tr>
<td><code>cout.precision(n)</code></td>
<td>设置浮点数精度（与 <code>fixed</code> 配合）</td>
</tr>
<tr>
<td><code>cout.width(n)</code></td>
<td>设置最小输出宽度（仅影响下一次）</td>
</tr>
<tr>
<td><code>cout.fill(ch)</code></td>
<td>设置填充字符（默认空格）</td>
</tr>
</tbody>
</table><pre><code class="prism language-cpp"><span class="token comment">// 示例：读取带空格的整行</span>
<span class="token keyword">char</span> buf<span class="token punctuation">[</span><span class="token number">100</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
cin<span class="token punctuation">.</span><span class="token function">getline</span><span class="token punctuation">(</span>buf<span class="token punctuation">,</span> <span class="token number">100</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
cout <span class="token operator">&lt;&lt;</span> buf <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>

<span class="token comment">// 示例：精细控制输出</span>
cout<span class="token punctuation">.</span><span class="token function">precision</span><span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
cout <span class="token operator">&lt;&lt;</span> fixed <span class="token operator">&lt;&lt;</span> <span class="token number">3.14159</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>  <span class="token comment">// 3.142</span>
cout<span class="token punctuation">.</span><span class="token function">width</span><span class="token punctuation">(</span><span class="token number">8</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
cout<span class="token punctuation">.</span><span class="token function">fill</span><span class="token punctuation">(</span><span class="token char">'0'</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
cout <span class="token operator">&lt;&lt;</span> right <span class="token operator">&lt;&lt;</span> <span class="token number">42</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>        <span class="token comment">// 00000042</span>
</code></pre>
<h4 id="读取整行（getline）"><span class="prefix"></span><span class="content">读取整行（<code>getline</code>）</span><span class="suffix"></span></h4>
<pre><code class="prism language-cpp">string line<span class="token punctuation">;</span>
<span class="token function">getline</span><span class="token punctuation">(</span>cin<span class="token punctuation">,</span> line<span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// 读取一整行，包括空格</span>
</code></pre>
<p>若 <code>getline</code> 之前使用了 <code>cin &gt;&gt;</code>，需先处理残留的换行符：</p>
<pre><code class="prism language-cpp">cin <span class="token operator">&gt;&gt;</span> n<span class="token punctuation">;</span>
<span class="token function">getline</span><span class="token punctuation">(</span>cin<span class="token punctuation">,</span> s<span class="token punctuation">)</span><span class="token punctuation">;</span>       <span class="token comment">// 会读取残留的换行符，读不到内容</span>
<span class="token comment">// 解决方法：</span>
cin<span class="token punctuation">.</span><span class="token function">ignore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>          <span class="token comment">// 忽略一个字符（换行符）</span>
<span class="token function">getline</span><span class="token punctuation">(</span>cin<span class="token punctuation">,</span> s<span class="token punctuation">)</span><span class="token punctuation">;</span>       <span class="token comment">// 正确读取</span>
</code></pre>
<h4 id="scanf--printf（c-风格格式化-io）"><span class="prefix"></span><span class="content">scanf / printf（C 风格格式化 I/O）</span><span class="suffix"></span></h4>
<p><code>scanf</code> 和 <code>printf</code> 使用格式控制字符串，需包含 <code>&lt;cstdio&gt;</code> 头文件，适合需要精确控制格式的场景。</p>
<pre><code class="prism language-cpp"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;cstdio&gt;</span></span>

<span class="token keyword">int</span> a<span class="token punctuation">;</span>
<span class="token keyword">double</span> b<span class="token punctuation">;</span>
<span class="token keyword">char</span> s<span class="token punctuation">[</span><span class="token number">100</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token function">scanf</span><span class="token punctuation">(</span><span class="token string">"%d%lf%s"</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>a<span class="token punctuation">,</span> <span class="token operator">&amp;</span>b<span class="token punctuation">,</span> s<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 读取</span>
<span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"a=%d, b=%.2f, s=%s\n"</span><span class="token punctuation">,</span> a<span class="token punctuation">,</span> b<span class="token punctuation">,</span> s<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre>
<p><strong>常用格式符：</strong></p>

<table>
<thead>
<tr>
<th>格式符</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>%d</code></td>
<td>int</td>
</tr>
<tr>
<td><code>%hd</code></td>
<td>short</td>
</tr>
<tr>
<td><code>%ld</code></td>
<td>long</td>
</tr>
<tr>
<td><code>%lld</code></td>
<td>long long</td>
</tr>
<tr>
<td><code>%u</code></td>
<td>unsigned int</td>
</tr>
<tr>
<td><code>%o</code></td>
<td>八进制整数</td>
</tr>
<tr>
<td><code>%x</code> / <code>%X</code></td>
<td>十六进制整数（小写/大写）</td>
</tr>
<tr>
<td><code>%f</code></td>
<td>float（输出）</td>
</tr>
<tr>
<td><code>%lf</code></td>
<td>double（输入用 <code>%lf</code>，输出可用 <code>%f</code>）</td>
</tr>
<tr>
<td><code>%.nf</code></td>
<td>保留 n 位小数的浮点数</td>
</tr>
<tr>
<td><code>%e</code> / <code>%E</code></td>
<td>科学计数法</td>
</tr>
<tr>
<td><code>%c</code></td>
<td>char</td>
</tr>
<tr>
<td><code>%s</code></td>
<td>char[] 字符串</td>
</tr>
<tr>
<td><code>%p</code></td>
<td>指针地址</td>
</tr>
<tr>
<td><code>%%</code></td>
<td>输出百分号 <code>%</code></td>
</tr>
</tbody>
</table><p><strong>修饰符：</strong></p>

<table>
<thead>
<tr>
<th>修饰符</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>%nd</code></td>
<td>输出宽度为 n，右对齐，空格填充</td>
</tr>
<tr>
<td><code>%-nd</code></td>
<td>输出宽度为 n，左对齐</td>
</tr>
<tr>
<td><code>%0nd</code></td>
<td>输出宽度为 n，前导零填充</td>
</tr>
<tr>
<td><code>%.nf</code></td>
<td>浮点数保留 n 位小数</td>
</tr>
<tr>
<td><code>%*d</code></td>
<td>宽度由参数指定，如 <code>printf("%*d", 8, 123);</code></td>
</tr>
</tbody>
</table><h4 id="常用转义字符"><span class="prefix"></span><span class="content">常用转义字符</span><span class="suffix"></span></h4>

<table>
<thead>
<tr>
<th>转义字符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>\n</code></td>
<td>换行</td>
</tr>
<tr>
<td><code>\t</code></td>
<td>水平制表符（Tab）</td>
</tr>
<tr>
<td><code>\r</code></td>
<td>回车</td>
</tr>
<tr>
<td><code>\a</code></td>
<td>响铃（Alert）</td>
</tr>
<tr>
<td><code>\b</code></td>
<td>退格</td>
</tr>
<tr>
<td><code>\f</code></td>
<td>换页</td>
</tr>
<tr>
<td><code>\v</code></td>
<td>垂直制表符</td>
</tr>
<tr>
<td><code>\0</code></td>
<td>空字符（字符串结束标志）</td>
</tr>
<tr>
<td><code>\\</code></td>
<td>反斜杠 <code>\</code></td>
</tr>
<tr>
<td><code>\'</code></td>
<td>单引号 <code>'</code></td>
</tr>
<tr>
<td><code>\"</code></td>
<td>双引号 <code>"</code></td>
</tr>
<tr>
<td><code>\xhh</code></td>
<td>十六进制 ASCII 码（如 <code>\x41</code> = ‘A’）</td>
</tr>
<tr>
<td><code>\ooo</code></td>
<td>八进制 ASCII 码（如 <code>\101</code> = ‘A’）</td>
</tr>
</tbody>
</table><pre><code class="prism language-cpp">cout <span class="token operator">&lt;&lt;</span> <span class="token string">"第一行\n第二行\t制表符\n"</span><span class="token punctuation">;</span>
cout <span class="token operator">&lt;&lt;</span> <span class="token string">"文件路径：C:\\Users\\name\n"</span><span class="token punctuation">;</span>
cout <span class="token operator">&lt;&lt;</span> <span class="token string">"双引号：\"Hello\"，十六进制 A：\x41\n"</span><span class="token punctuation">;</span>
</code></pre>

