---


---

<h3 id="字符串流"><span class="prefix"></span><span class="content">字符串流</span><span class="suffix"></span></h3>
<hr>
<p>C++ 的 <code>&lt;sstream&gt;</code> 提供了基于字符串的流操作，用于在内存中对字符串进行格式化输入输出。</p>
<h4 id="基本使用"><span class="prefix"></span><span class="content">基本使用</span><span class="suffix"></span></h4>
<pre><code class="prism language-cpp"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;sstream&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;iostream&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;string&gt;</span></span>
<span class="token keyword">using</span> <span class="token keyword">namespace</span> std<span class="token punctuation">;</span>

<span class="token comment">// 1. 输出到字符串（类似 cout）</span>
ostringstream oss<span class="token punctuation">;</span>
oss <span class="token operator">&lt;&lt;</span> <span class="token string">"Hello "</span> <span class="token operator">&lt;&lt;</span> <span class="token number">2024</span> <span class="token operator">&lt;&lt;</span> <span class="token string">"!"</span><span class="token punctuation">;</span>
string s <span class="token operator">=</span> oss<span class="token punctuation">.</span><span class="token function">str</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>   <span class="token comment">// s = "Hello 2024!"</span>

<span class="token comment">// 2. 从字符串读取（类似 cin）</span>
string data <span class="token operator">=</span> <span class="token string">"100 3.14 Hello"</span><span class="token punctuation">;</span>
istringstream <span class="token function">iss</span><span class="token punctuation">(</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">int</span> a<span class="token punctuation">;</span> <span class="token keyword">double</span> b<span class="token punctuation">;</span> string c<span class="token punctuation">;</span>
iss <span class="token operator">&gt;&gt;</span> a <span class="token operator">&gt;&gt;</span> b <span class="token operator">&gt;&gt;</span> c<span class="token punctuation">;</span>     <span class="token comment">// a=100, b=3.14, c="Hello"</span>

<span class="token comment">// 3. 可读可写</span>
stringstream ss<span class="token punctuation">;</span>
ss <span class="token operator">&lt;&lt;</span> <span class="token number">42</span> <span class="token operator">&lt;&lt;</span> <span class="token string">" "</span> <span class="token operator">&lt;&lt;</span> <span class="token number">3.14</span><span class="token punctuation">;</span>
<span class="token keyword">int</span> x<span class="token punctuation">;</span> <span class="token keyword">double</span> y<span class="token punctuation">;</span>
ss <span class="token operator">&gt;&gt;</span> x <span class="token operator">&gt;&gt;</span> y<span class="token punctuation">;</span>           <span class="token comment">// x=42, y=3.14</span>
</code></pre>
<hr>
<h4 id="清空与重置"><span class="prefix"></span><span class="content">清空与重置</span><span class="suffix"></span></h4>
<pre><code class="prism language-cpp">stringstream ss<span class="token punctuation">;</span>
ss <span class="token operator">&lt;&lt;</span> <span class="token string">"Hello"</span><span class="token punctuation">;</span>
ss<span class="token punctuation">.</span><span class="token function">str</span><span class="token punctuation">(</span><span class="token string">""</span><span class="token punctuation">)</span><span class="token punctuation">;</span>           <span class="token comment">// 清空内容</span>
ss<span class="token punctuation">.</span><span class="token function">clear</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>           <span class="token comment">// 重置状态标志（重要！）</span>
ss <span class="token operator">&lt;&lt;</span> <span class="token string">"World"</span><span class="token punctuation">;</span>        <span class="token comment">// 重新使用</span>
</code></pre>
<p><strong>注意</strong>：<code>str("")</code> 清空内容，<code>clear()</code> 清除错误状态（如 EOF 标志），两者通常配合使用。</p>
<hr>
<h4 id="常用操作"><span class="prefix"></span><span class="content">常用操作</span><span class="suffix"></span></h4>

<table>
<thead>
<tr>
<th>操作</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>oss.str()</code></td>
<td>获取 <code>oss</code> 中的字符串内容</td>
</tr>
<tr>
<td><code>oss.str(new_str)</code></td>
<td>将 <code>oss</code> 的内容替换为 <code>new_str</code></td>
</tr>
<tr>
<td><code>iss &gt;&gt; var</code></td>
<td>从 <code>iss</code> 中读取数据到变量</td>
</tr>
<tr>
<td><code>oss &lt;&lt; expr</code></td>
<td>将表达式输出到 <code>oss</code> 中</td>
</tr>
</tbody>
</table><hr>
<h4 id="典型应用场景"><span class="prefix"></span><span class="content">典型应用场景</span><span class="suffix"></span></h4>
<ul>
<li>字符串拼接与格式化（替代 <code>to_string()</code> + 手动拼接）</li>
<li>解析带分隔符或混合类型的数据</li>
<li>类型转换（如 <code>string</code> 转 <code>int</code>/<code>double</code>）</li>
</ul>

