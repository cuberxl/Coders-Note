
## $\LaTeX$ 关系符与常用符号总表

> 说明：`amssymb` 宏包能覆盖大部分符号；带 `*` 的通常需要 `amssymb` 或 `mathtools`。
---

## 一、基本比较

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `<` | $<$ | 小于 |
| `>` | $>$ | 大于 |
| `\le` / `\leq` | $\le$ | 小于等于 |
| `\ge` / `\geq` | $\ge$ | 大于等于 |
| `\ll` | $\ll$ | 远小于 |
| `\gg` | $\gg$ | 远大于 |
| `\lll`* | $\lll$ | 更远小于 |
| `\ggg`* | $\ggg$ | 更远大于 |
| `\leqslant`* | $\leqslant$ | 斜杠小于等于 |
| `\geqslant`* | $\geqslant$ | 斜杠大于等于 |
| `<=` | $<=$ | 编程写法，不推荐 |
| `>=` | $>=$ | 编程写法，不推荐 |

---

## 二、否定比较

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\nless`* | $\nless$ | 不小于 |
| `\ngtr`* | $\ngtr$ | 不大于 |
| `\nleq`* | $\nleq$ | 不小于等于 |
| `\ngeq`* | $\ngeq$ | 不大于等于 |
| `\nleqslant`* | $\nleqslant$ | 斜杠不小于等于 |
| `\ngeqslant`* | $\ngeqslant$ | 斜杠不大于等于 |
| `\not<` | $\not<$ | 手动否定 |
| `\not>` | $\not>$ | 手动否定 |
| `\not\le` | $\not\le$ | 手动否定 |
| `\not\ge` | $\not\ge$ | 手动否定 |

---

## 三、近似与变体

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\approx` | $\approx$ | 约等于 |
| `\not\approx` | $\not\approx$ | 不约等于 |
| `\simeq` | $\simeq$ | 渐近等于 |
| `\cong` | $\cong$ | 全等 / 同余 |
| `\sim` | $\sim$ | 相似 |
| `\nsim`* | $\nsim$ | 不相似 |
| `\lesssim`* | $\lesssim$ | 小于且近似 |
| `\gtrsim`* | $\gtrsim$ | 大于且近似 |
| `\lessapprox`* | $\lessapprox$ | 小于且约等于 |
| `\gtrapprox`* | $\gtrapprox$ | 大于且约等于 |
| `\lessgtr`* | $\lessgtr$ | 小于或大于 |
| `\gtrless`* | $\gtrless$ | 大于或小于 |
| `\lesseqgtr`* | $\lesseqgtr$ | 小于等于或大于 |
| `\gtreqless`* | $\gtreqless$ | 大于等于或小于 |

---

## 四、等价与同余

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\equiv` | $\equiv$ | 恒等 / 同余 |
| `\not\equiv` | $\not\equiv$ | 不同余 |
| `\pmod{m}` | $\pmod{m}$ | 模 m，带括号 |
| `\bmod` | $\bmod$ | 二元取模 |
| `\mod` | $\mod$ | 普通 mod |
| `\pod{m}` | $\pod{m}$ | 只带括号 |
| `\mid` | $\mid$ | 整除 |
| `\nmid`* | $\nmid$ | 不整除 |
| `\parallel` | $\parallel$ | 平行 / 可比 |
| `\nparallel`* | $\nparallel$ | 不平行 / 不可比 |
| `\perp` | $\perp$ | 正交 / 不可比 |

---

## 五、偏序与格

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\preceq` | $\preceq$ | 偏序 |
| `\succeq` | $\succeq$ | 反向偏序 |
| `\prec` | $\prec$ | 严格偏序 |
| `\succ` | $\succ$ | 反向严格偏序 |
| `\npreceq`* | $\npreceq$ | 不偏序 |
| `\nsucceq`* | $\nsucceq$ | 不反向偏序 |
| `\sqsubseteq` | $\sqsubseteq$ | 方格偏序 |
| `\sqsupseteq` | $\sqsupseteq$ | 反向方格偏序 |
| `\sqsubset` | $\sqsubset$ | 严格方格偏序 |
| `\sqsupset` | $\sqsupset$ | 反向严格方格偏序 |
| `\trianglelefteq`* | $\trianglelefteq$ | 三角偏序 |
| `\trianglerighteq`* | $\trianglerighteq$ | 反向三角偏序 |
| `\triangleleft` | $\triangleleft$ | 三角左 |
| `\triangleright` | $\triangleright$ | 三角右 |

---

## 六、集合关系

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\in` | $\in$ | 属于 |
| `\notin` | $\notin$ | 不属于 |
| `\ni` | $\ni$ | 反向属于 |
| `\subset` | $\subset$ | 真子集 |
| `\subseteq` | $\subseteq$ | 子集 |
| `\subsetneq`* | $\subsetneq$ | 真子集 |
| `\supset` | $\supset$ | 真超集 |
| `\supseteq` | $\supseteq$ | 超集 |
| `\supsetneq`* | $\supsetneq$ | 真超集 |
| `\nsubseteq`* | $\nsubseteq$ | 非子集 |
| `\nsupseteq`* | $\nsupseteq$ | 非超集 |
| `\cup` | $\cup$ | 并 |
| `\cap` | $\cap$ | 交 |
| `\setminus` | $\setminus$ | 差集 |
| `\varnothing` | $\varnothing$ | 空集 |
| `\emptyset` | $\emptyset$ | 空集 |
| `\complement`* | $\complement$ | 补集 |

---

## 七、箭头

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\to` | $\to$ | 右箭头 |
| `\leftarrow` | $\leftarrow$ | 左箭头 |
| `\rightarrow` | $\rightarrow$ | 右箭头 |
| `\leftrightarrow` | $\leftrightarrow$ | 双向 |
| `\Rightarrow` | $\Rightarrow$ | 推出 |
| `\Leftarrow` | $\Leftarrow$ | 反向推出 |
| `\Leftrightarrow` | $\Leftrightarrow$ | 等价 |
| `\longrightarrow` | $\longrightarrow$ | 长右箭头 |
| `\longleftarrow` | $\longleftarrow$ | 长左箭头 |
| `\Longrightarrow` | $\Longrightarrow$ | 长推出 |
| `\Longleftarrow` | $\Longleftarrow$ | 长反向推出 |
| `\Longleftrightarrow` | $\Longleftrightarrow$ | 长等价 |
| `\mapsto` | $\mapsto$ | 映射到 |
| `\longmapsto` | $\longmapsto$ | 长映射到 |
| `\uparrow` | $\uparrow$ | 上箭头 |
| `\downarrow` | $\downarrow$ | 下箭头 |
| `\updownarrow` | $\updownarrow$ | 上下 |
| `\Uparrow` | $\Uparrow$ | 双上 |
| `\Downarrow` | $\Downarrow$ | 双下 |
| `\Updownarrow` | $\Updownarrow$ | 双上下 |
| `\nearrow` | $\nearrow$ | 右上 |
| `\searrow` | $\searrow$ | 右下 |
| `\swarrow` | $\swarrow$ | 左下 |
| `\nwarrow` | $\nwarrow$ | 左上 |
| `\hookrightarrow` | $\hookrightarrow$ | 嵌入 |
| `\hookleftarrow` | $\hookleftarrow$ | 反向嵌入 |
| `\rightharpoonup` | $\rightharpoonup$ | 右半箭头 |
| `\leftharpoonup` | $\leftharpoonup$ | 左半箭头 |
| `\rightleftharpoons` | $\rightleftharpoons$ | 可逆 |
| `\leadsto`* | $\leadsto$ | 导出 |

---

## 八、逻辑

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\forall` | $\forall$ | 任意 |
| `\exists` | $\exists$ | 存在 |
| `\nexists`* | $\nexists$ | 不存在 |
| `\neg` | $\neg$ | 非 |
| `\lnot` | $\lnot$ | 非 |
| `\land` | $\land$ | 与 |
| `\lor` | $\lor$ | 或 |
| `\wedge` | $\wedge$ | 与 / 交 |
| `\vee` | $\vee$ | 或 / 并 |
| `\implies`* | $\implies$ | 推出 |
| `\impliedby`* | $\impliedby$ | 反向推出 |
| `\iff`* | $\iff$ | 当且仅当 |
| `\top` | $\top$ | 真 / 顶 |
| `\bot` | $\bot$ | 假 / 底 |
| `\vdash` | $\vdash$ | 可证 |
| `\dashv` | $\dashv$ | 反向可证 |
| `\models` | $\models$ | 满足 |
| `\vDash` | $\vDash$ | 模型满足 |
| `\Vdash` | $\Vdash$ | 强制 |
| `\nvdash`* | $\nvdash$ | 不可证 |
| `\nvDash`* | $\nvDash$ | 不满足 |

---

## 九、大型运算符

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\sum` | $\sum$ | 求和 |
| `\prod` | $\prod$ | 连乘 |
| `\coprod` | $\coprod$ | 余积 |
| `\int` | $\int$ | 积分 |
| `\iint` | $\iint$ | 二重积分 |
| `\iiint` | $\iiint$ | 三重积分 |
| `\oint` | $\oint$ | 环路积分 |
| `\oiint`* | $\oiint$ | 闭合曲面积分 |
| `\bigcup` | $\bigcup$ | 大并 |
| `\bigcap` | $\bigcap$ | 大交 |
| `\bigvee` | $\bigvee$ | 大或 |
| `\bigwedge` | $\bigwedge$ | 大与 |
| `\bigoplus` | $\bigoplus$ | 直和 |
| `\bigotimes` | $\bigotimes$ | 张量积 |
| `\bigodot` | $\bigodot$ | 圆点积 |
| `\bigsqcup` | $\bigsqcup$ | 不交并 |
| `\biguplus` | $\biguplus$ | 多重并 |

---

## 十、微积分与算子

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\partial` | $\partial$ | 偏导 |
| `\nabla` | $\nabla$ | 梯度 |
| `\infty` | $\infty$ | 无穷 |
| `\lim` | $\lim$ | 极限 |
| `\limsup` | $\limsup$ | 上极限 |
| `\liminf` | $\liminf$ | 下极限 |
| `\sup` | $\sup$ | 上确界 |
| `\inf` | $\inf$ | 下确界 |
| `\max` | $\max$ | 最大 |
| `\min` | $\min$ | 最小 |
| `\arg` | $\arg$ | 辐角 |
| `\det` | $\det$ | 行列式 |
| `\dim` | $\dim$ | 维数 |
| `\ker` | $\ker$ | 核 |
| `\deg` | $\deg$ | 次数 |
| `\gcd` | $\gcd$ | 最大公约数 |
| `\Pr` | $\Pr$ | 概率 |
| `\log` | $\log$ | 对数 |
| `\ln` | $\ln$ | 自然对数 |
| `\exp` | $\exp$ | 指数 |
| `\sin` | $\sin$ | 正弦 |
| `\cos` | $\cos$ | 余弦 |
| `\tan` | $\tan$ | 正切 |
| `\operatorname{lcm}` | $\operatorname{lcm}$ | 最小公倍数 |

---

## 十一、点与省略

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\cdot` | $\cdot$ | 点乘 |
| `\cdots` | $\cdots$ | 居中省略 |
| `\ldots` | $\ldots$ | 底部省略 |
| `\vdots` | $\vdots$ | 竖向省略 |
| `\ddots` | $\ddots$ | 对角省略 |
| `\dots` | $\dots$ | 自动省略 |
| `\iddots`* | $\iddots$ | 反向对角省略 |
| `\dotsb`* | $\dotsb$ | 二元省略 |
| `\dotsc`* | $\dotsc$ | 逗号省略 |
| `\dotsi`* | $\dotsi$ | 积分省略 |
| `\dotsm`* | $\dotsm$ | 乘法省略 |
| `\dotso`* | $\dotso$ | 其他省略 |

---

## 十二、括号与定界符

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\left( ... \right)` | $\left( ... \right)$ | 自适应圆括号 |
| `\left[ ... \right]` | $\left[ ... \right]$ | 自适应方括号 |
| `\left\{ ... \right\}` | $\left\{ ... \right\}$ | 自适应花括号 |
| `\left| ... \right|` | $\left| ... \right|$ | 自适应竖线 |
| `\left\| ... \right\|` | $\left\| ... \right\|$ | 自适应双竖线 |
| `\lfloor ... \rfloor` | $\lfloor ... \rfloor$ | 下取整 |
| `\lceil ... \rceil` | $\lceil ... \rceil$ | 上取整 |
| `\langle ... \rangle` | $\langle ... \rangle$ | 尖括号 |
| `\lvert ... \rvert` | $\lvert ... \rvert$ | 竖线 |
| `\lVert ... \rVert` | $\lVert ... \rVert$ | 双竖线 |

---

## 十三、转义字符

| 字符 | 转义 | 效果 |
| :--- | :--- | :--- |
| `#` | `\#` | # |
| `$` | `\$` | $ |
| `%` | `\%` | % |
| `&` | `\&` | & |
| `_` | `\_` | _ |
| `{` | `\{` | { |
| `}` | `\}` | } |
| `~` | `\~{}` | ~ |
| `^` | `\^{}` | ^ |
| `\` | `\backslash` | \ |
| `\` | `\textbackslash` | \ |
| `|` | `\vert` | $\vert$ |
| `‖` | `\Vert` | $\Vert$ |

---

## 十四、空格

| 写法 | 效果 | 说明 |
| :--- | :--- | :--- |
| `\,` | $\,$ | 最窄 |
| `\ ` | $\ $ | 常用 |
| `\quad` | $\quad$ | 1 字符 |
| `\qquad` | $\qquad$ | 2 字符 |
| `\!` | $\!$ | 负空格 |

---

## 十五、可复制模板

```latex
$$
a \le b, \quad a \ge b, \quad a \ll b, \quad a \gg b
$$

$$
a \equiv b \pmod{m}, \quad a \bmod m = r
$$

$$
a \preceq b, \quad a \prec b, \quad a \sqsubseteq b
$$

$$
x \in A, \quad A \subseteq B, \quad A \cup B, \quad A \cap B
$$

$$
P \Rightarrow Q, \quad P \Leftrightarrow Q, \quad x \mapsto f(x)
$$

$$
\forall x \in A,\ \exists y \in B,\ x < y
$$

$$
\sum_{i=1}^{n} i, \quad \prod_{i=1}^{n} i, \quad \int_a^b f(x)\,dx
$$

$$
\lim_{x \to 0} f(x), \quad \frac{\partial f}{\partial x}, \quad \nabla f
$$

$$
a \mid b, \quad a \nmid b, \quad m \parallel n, \quad m \perp n
$$

$$
\sup A, \quad \inf A, \quad \max A, \quad \min A
$$

$$
a \vee b, \quad a \wedge b, \quad \top, \quad \bot
$$
```

---

## 十六、踩坑提醒

- `<=`、`>=` 在 LaTeX 里不是小于等于、大于等于。
- 小于等于用 `\le`，大于等于用 `\ge`。
- 偏序优先用 `\preceq`、`\prec`、`\sqsubseteq`。
- `\mid` 是整除，`\vert` 是竖线，别混。
- `\pmod{m}` 多字符模数要加 `{}`。
- `\not\le` 和 `\nleq` 视觉略有差异，看宏包。
- 带 `*` 的符号通常需要 `amssymb`。
- 文本里写 `\` 用 `\textbackslash`，数学里用 `\backslash`。
- `\\` 多数时候是换行，不是显示反斜杠。
<!--stackedit_data:
eyJoaXN0b3J5IjpbMTc0Nzc0Nzg5MV19
-->